Goal: a student can moderate a focus group of simulated personas who react to each other, following the same funnel and producing the same memo fields as PA3.5, without being able to spend real money by accident.

## Outcomes

- [x] A student can start a focus group with at least three personas and cannot start one with fewer — the control refuses two, and the refusal says why. — check: `cd ../../apps/web && npm run test:unit`
- [x] The student types the question. Every persona in the room answers it, and each answer is attributed to a named persona the student picked. — check: `cd ../.. && ./apps/api/.venv/bin/python -m pytest apps/api/tests/test_focus_group.py -q -k 'every_persona_answers_attributed'`
- [x] From the second question onward, each persona is given the other personas' answers to the earlier questions, so a persona can agree, disagree, or build on what another said. A persona never sees its own future turns or another room's transcript. — check: `cd ../.. && ./apps/api/.venv/bin/python -m pytest apps/api/tests/test_focus_group.py -q -k 'personas_see_each_other_not_other_rooms'`
- [x] The session walks the five PA3.5 stages in order — icebreaker, general space needs, the Tahoe Mini concept, price reactions, close — and the student can see which stage they are in and move back to a previous one without losing answers already collected. — check: `cd ../../apps/web && npm run test:unit`
- [x] Price is never anchored first: the app does not surface a price to the personas before the price-reactions stage. — check: `cd ../.. && ./apps/api/.venv/bin/python -m pytest apps/api/tests/test_focus_group.py -q -k 'price_not_anchored_before_stage'`
- [x] At the end the student gets the PA3.5 memo fields off their own transcript: themes with a verbatim quote each, one surprise, and at least three closed-ended answer options phrased in participant language. — check: `cd ../.. && ./apps/api/.venv/bin/python -m pytest apps/api/tests/test_focus_group.py -q -k 'memo_fields'`
- [x] Every quote and every answer option is verbatim from a transcript turn the student can locate, not paraphrased or invented. — check: `cd ../.. && ./apps/api/.venv/bin/python -m pytest apps/api/tests/test_focus_group.py -q -k 'verbatim_traceable'`
- [x] Before a focus group starts, the student sees what it will cost — personas times rounds, not one call — and confirms it. Dismissing the confirmation starts nothing and spends nothing. — check: `cd ../../apps/web && npm run test:unit`
- [x] A focus group cannot reach an expensive model without the expensive-model box checked; raising the persona count alone can never get there. — check: `cd ../../apps/web && npm run test:unit`
- [x] Focus-group calls spend through the existing run and class budgets, and a billed-but-unusable response still records its cost. A budget stop leaves every answer that was actually charged visible and in order. — check: `cd ../.. && ./apps/api/.venv/bin/python -m pytest apps/api/tests/test_focus_group.py -q -k 'budget'`
- [x] Re-opening a finished focus group, or its memo, makes no new paid call. — check: `cd ../.. && ./apps/api/.venv/bin/python -m pytest apps/api/tests/test_focus_group.py -q -k 'cached'`
- [x] A provider failure mid-room leaves the answers already collected readable and offers an explicit retry, rather than an empty success or a stuck spinner. — check: `cd ../.. && ./apps/api/.venv/bin/python -m pytest apps/api/tests/test_focus_group.py -q -k 'partial_failure_recoverable'`
- [x] The student can export the transcript and the memo for submission, with each turn attributed to its persona. — check: `cd ../.. && ./apps/api/.venv/bin/python -m pytest apps/api/tests/test_focus_group.py -q -k 'export'`
- [x] Students in classroom no-login mode can run a focus group and get its memo without hitting a login wall. — check: `cd ../../apps/web && npm run test:unit`
- [x] Nothing already working broke — the interview section, model comparison, follow-up chat, batch and regenerate controls, standalone themes and the pre-recorded interviews page all still work. — check: `cd ../../apps/web && npm run test:unit && cd ../.. && ./apps/api/.venv/bin/python -m pytest apps/api/tests/ -q`

## Don't touch

- the main gated workflow and its insights
- `scripts/prerecord_interviews.py`
- the budget constants, `enforce_run_preflight`, `enforce_measured_cost`
- `docs/cost-report.md` and `apps/api/tests/test_cost_report.py`
- no teacher/student role or permission system — Lin asked for a checkbox
- do not build AI-interviews-the-student; out of scope

## Added by Sol refute (each needs a check before it can pass)
- [x] (sol) A student cannot select the same persona twice into one room, and the room refuses or de-duplicates rather than producing two identically-named speakers the memo cannot tell apart. — check: `cd ../.. && ./apps/api/.venv/bin/python -m pytest apps/api/tests/test_focus_group.py -q -k 'duplicate_personas_refused'`
- [x] (sol) There is an upper bound on personas and rounds per room, and exceeding it is refused before any call is made rather than allowed to run up an arbitrarily large bill. — check: `cd ../.. && ./apps/api/.venv/bin/python -m pytest apps/api/tests/test_focus_group.py -q -k 'upper_bound'`
- [x] (sol) Double-clicking start, or submitting the same question twice, produces one round of answers and one charge, not two rooms or a duplicated round. — check: `cd ../.. && ./apps/api/.venv/bin/python -m pytest apps/api/tests/test_focus_group.py -q -k 'idempotent'`
- [x] (sol) Submitting a new question while the previous round is still generating is either blocked or queued, and never interleaves the two rounds' answers into the transcript out of order. — check: `cd ../.. && ./apps/api/.venv/bin/python -m pytest apps/api/tests/test_focus_group.py -q -k 'concurrent'`
- [x] (sol) A student who closes the tab or refreshes mid-session returns to the same room with every answer already collected and charged still present and the stage indicator where they left it. — check: `cd ../.. && ./apps/api/.venv/bin/python -m pytest apps/api/tests/test_focus_group.py -q -k 'resume'`
- [x] (sol) The student can cancel a room in progress, and cancellation leaves the already-charged answers readable and makes no further paid calls. — check: `cd ../.. && ./apps/api/.venv/bin/python -m pytest apps/api/tests/test_focus_group.py -q -k 'cancel'`
- [x] (sol) When one persona fails while the others succeed, the transcript shows which persona is missing an answer instead of silently shipping a short round that reads as complete. — check: `cd ../.. && ./apps/api/.venv/bin/python -m pytest apps/api/tests/test_focus_group.py -q -k 'missing_persona_named'`
- [x] (sol) Retrying after a provider failure re-runs only the missing turns and does not re-charge or duplicate answers already collected. — check: `cd ../.. && ./apps/api/.venv/bin/python -m pytest apps/api/tests/test_focus_group.py -q -k 'retry_only_missing'`
- [x] (sol) After the run the student sees what it actually cost against what the confirmation estimated, so an under-estimate is visible rather than silent. — check: `cd ../.. && ./apps/api/.venv/bin/python -m pytest apps/api/tests/test_focus_group.py -q -k 'actual_vs_estimated'`
- [x] (sol) The pre-start cost estimate updates when the student changes persona count, rounds, or model before they confirm, so the number they approve is the one they will be charged. — check: `cd ../../apps/web && npm run test:unit`
- [x] (sol) A budget stop tells the student in plain words that the budget stopped it and what remains, rather than presenting as a generic error or an empty room. — check: `cd ../.. && ./apps/api/.venv/bin/python -m pytest apps/api/tests/test_focus_group.py -q -k 'plain_words'`
- [x] (sol) Going back to an earlier stage and asking a different question adds to the transcript without overwriting or orphaning the answers already collected at that stage. — check: `cd ../.. && ./apps/api/.venv/bin/python -m pytest apps/api/tests/test_focus_group.py -q -k 'revisit_stage'`
- [x] (sol) Stages cannot be skipped forward past the price-reactions stage in a way that produces a memo from a transcript that never reached the concept or price stages. — check: `cd ../.. && ./apps/api/.venv/bin/python -m pytest apps/api/tests/test_focus_group.py -q -k 'stage_order_enforced'`
- [x] (sol) A room with too few turns to support the memo fields says so explicitly instead of emitting a memo with invented, empty, or padded themes, surprise, or answer options. — check: `cd ../.. && ./apps/api/.venv/bin/python -m pytest apps/api/tests/test_focus_group.py -q -k 'memo_requires_enough'`
- [x] (sol) A finished focus group appears in the student's list of past runs and can be re-opened, exported, and deleted like their other work. — check: `cd ../.. && ./apps/api/.venv/bin/python -m pytest apps/api/tests/test_focus_group.py -q -k 'room_list_lifecycle'`
- [x] (sol) Focus-group transcripts are written to their own lane and never appear in the interview section's lists, the standalone themes inputs, or the pre-recorded interviews page. — check: `cd ../.. && ./apps/api/.venv/bin/python -m pytest apps/api/tests/test_focus_group.py -q -k 'separate_lane'`
- [x] (sol) In classroom no-login mode, one student's room and memo are not visible to the next student on the same device or to other students in the class. — check: `cd ../../apps/web && npm run test:unit && cd ../.. && ./apps/api/.venv/bin/python -m pytest apps/api/tests/test_focus_group.py -q -k 'classroom_isolation'`
- [x] (sol) A failed or budget-stopped room writes a log entry carrying a room identifier, stage, persona, and provider error that the on-call engineer can match to the student's report. — check: `cd ../.. && ./apps/api/.venv/bin/python -m pytest apps/api/tests/test_focus_group.py -q -k 'failure_log'`
- [x] (sol) A persona call that hangs times out with a bounded wait and surfaces as a retryable failure rather than an indefinite spinner. — check: `cd ../.. && ./apps/api/.venv/bin/python -m pytest apps/api/tests/test_focus_group.py -q -k 'timeout'`
- [x] (sol) Export works on an unfinished or partially-failed room and marks it as incomplete rather than refusing or presenting a partial transcript as final. — check: `cd ../.. && ./apps/api/.venv/bin/python -m pytest apps/api/tests/test_focus_group.py -q -k 'marked_incomplete'`
